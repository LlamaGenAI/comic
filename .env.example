# LlamaGen API key used by smoke script
LLAMAGEN_API_KEY=YOUR_API_KEY

# Optional: override prompt for smoke run
SMOKE_PROMPT=A superhero cat saving a city from giant mice

# Optional: polling controls (ms)
SMOKE_INTERVAL_MS=5000
SMOKE_TIMEOUT_MS=180000
